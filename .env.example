# Copy to .env and adjust for your environment.
# Local Homebrew PostgreSQL (trust auth) example:
DATABASE_URL="postgresql://USER@localhost:5432/the_sies_files"
# Storyteller recovery bootstrap secret (Slice 6). Use a long random value in production.
STORYTELLER_RECOVERY_SECRET="replace-with-long-random-secret"
